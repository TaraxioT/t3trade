# The chart becomes the work surface — execution prompts

Authoritative plan: unified-graph-ui-plan.html

## U0 — Approve the chart-first direction (inside F0)

You are implementing T3 Forge for ETHOnline in /Users/george/Workspace/t3trade. This is a hackathon build scoped to September 9–11, 2026. Read AGENTS.md and the authoritative artifacts/reports/t3-forge-20260909/implementation-plan.html, plus the independent unified-graph-ui-plan.html. The user's current Forge direction supersedes the older continuity/fork-rehearsal, broad-polish, no-Uniswap and obsolete Luna-development mandates only where explicitly stated. Do not execute those old campaigns.

Use GPT/GLM for development and direct focused checks; the in-app provider is used only to exercise the product's real agent capability. Read .repos/effect-smol/LLMS.md before Effect-heavy changes. Current upstream-merge work is unrelated: preserve it, avoid compatibility archaeology, stage only your intended paths. No PR, push, production deployment or mainnet signing. Public mainnet reads are allowed; only the configured Uniswap public testnet may be written. Do not repurpose Hyperliquid signers or weaken any existing trading guard.

All product numbers and transaction evidence must be real. Fixtures belong only to automated tests. Authentic historical windows are explicitly historical and cannot drive live fee policy. Missing access/data produces a named unavailable state. No Node vm/host import fallback for generated code. No model-authored unrestricted Solidity. One approved fixed hook only.

Use the phase's listed file ownership; treat new names as intended targets, verify the actual repository seams. Existing shared schemas/RPCs must be updated across server, web, desktop and client-runtime; mobile only as needed to avoid broken shared compilation. Add focused tests and targeted package checks. For user-visible changes follow test-t3-app using one retained isolated stack/browser for the entire campaign, owned by the designated tester. Do not test development by asking the application to modify this repository.

Write progress/evidence under /tmp/t3-forge-20260909/U0/ with exact checkout, tested SHA, commands/cwds/exits, actual receipts, redacted screenshots and open failures. Record 'implemented', 'automated verified', 'real-data verified' and 'human accepted' separately. Never mark a phase accepted yourself. When checks pass, commit only the phase implementation and durable behavior docs (not temporary evidence). Return the concrete human verification steps and wait for the human's actual pass/fail before beginning any dependent phase. If the gate fails, repair this phase and repeat its relevant checks. An elapsed timeout is not approval.

U0 — Approve the chart-first direction (inside F0)
This UI phase is part of the main campaign, not extra schedule.

Read MarketChartPanel, MissionPriceChart, ThreadMarketCard/Panel, ChatView integration, trading.css and existing state hooks. Inspect the current app only if the single retained tester stack is already available; otherwise use inspected source/historical evidence and label that limit. Own no production UI edits yet. Compare the schematic with the existing shell at 1440×900, 1920×1080 and 1024px. Freeze chart-first two-pane layout, source labeling, legacy Research menu and persistent controls for active exposure. Identify exact insertion points and any minimum split-layout blocker; do not launch a competing app stack. Return one decisive layout, not several style options.

Expected result:
An agreed instrument/conversation/inspector composition and exact component ownership; no new route or visual framework.

Human verification, then stop:
George identifies where to ask, where the new capability appears, which network is observed, which is controlled and how to stop it. Approve the direction or name one blocking layout change before U1.

---

## U1 — Implement the instrument and reveal (inside F4)

You are implementing T3 Forge for ETHOnline in /Users/george/Workspace/t3trade. This is a hackathon build scoped to September 9–11, 2026. Read AGENTS.md and the authoritative artifacts/reports/t3-forge-20260909/implementation-plan.html, plus the independent unified-graph-ui-plan.html. The user's current Forge direction supersedes the older continuity/fork-rehearsal, broad-polish, no-Uniswap and obsolete Luna-development mandates only where explicitly stated. Do not execute those old campaigns.

Use GPT/GLM for development and direct focused checks; the in-app provider is used only to exercise the product's real agent capability. Read .repos/effect-smol/LLMS.md before Effect-heavy changes. Current upstream-merge work is unrelated: preserve it, avoid compatibility archaeology, stage only your intended paths. No PR, push, production deployment or mainnet signing. Public mainnet reads are allowed; only the configured Uniswap public testnet may be written. Do not repurpose Hyperliquid signers or weaken any existing trading guard.

All product numbers and transaction evidence must be real. Fixtures belong only to automated tests. Authentic historical windows are explicitly historical and cannot drive live fee policy. Missing access/data produces a named unavailable state. No Node vm/host import fallback for generated code. No model-authored unrestricted Solidity. One approved fixed hook only.

Use the phase's listed file ownership; treat new names as intended targets, verify the actual repository seams. Existing shared schemas/RPCs must be updated across server, web, desktop and client-runtime; mobile only as needed to avoid broken shared compilation. Add focused tests and targeted package checks. For user-visible changes follow test-t3-app using one retained isolated stack/browser for the entire campaign, owned by the designated tester. Do not test development by asking the application to modify this repository.

Write progress/evidence under /tmp/t3-forge-20260909/U1/ with exact checkout, tested SHA, commands/cwds/exits, actual receipts, redacted screenshots and open failures. Record 'implemented', 'automated verified', 'real-data verified' and 'human accepted' separately. Never mark a phase accepted yourself. When checks pass, commit only the phase implementation and durable behavior docs (not temporary evidence). Return the concrete human verification steps and wait for the human's actual pass/fail before beginning any dependent phase. If the gate fails, repair this phase and repeat its relevant checks. An elapsed timeout is not approval.

U1 — Implement the instrument and reveal (inside F4)
This UI phase is part of the main campaign, not extra schedule.

Own the bounded chart/thread integration, new ForgeSignalLane, ForgeBuildReceipt, ForgePoolInspector, ForgeVersionDiff, related web state and trading.css. Read implementation F1–F3 contracts; use actual typed server state. Implement the composition and interactions from this UI plan, keep existing source/legacy scene semantics, and never introduce mocked runtime samples. Build progress comes from persisted receipts; install adds the signal lane without moving zoom/composer. Source evidence, generated code/tests and policy receipts are progressive disclosure in one inspector. Installed-vs-confirmed versions remain separate.
Use current semantic tokens and font. No new chart library, animated graph background or extra sidebar brand. At 1024px collapse the inspector; preserve the conversation/composer. Long labels, errors, historical mode and missing data must fit. Active HL exposure retains direct controls. Follow test-t3-app for every user-visible change, using the campaign's sole retained tester. Run focused component/state tests and targeted web check, then commit explicit owned files. Produce screenshots of real data/state and record paths under /tmp/t3-forge-20260909/U1/.

Expected result:
One stable chart-first screen showing real source → build → installed signal → pool policy, with direct evidence inspection and source-aware labels.

Human verification, then stop:
George runs the first request and opens each evidence link; confirms chart/composer stay visible, source/target networks are distinct, a pending fee is not labeled effective, keyboard navigation works, and unavailable data remains readable. Check three viewport widths and active-position controls. Actual pass is required.

---

## U2 — Finish visual and interaction acceptance (inside F6)

You are implementing T3 Forge for ETHOnline in /Users/george/Workspace/t3trade. This is a hackathon build scoped to September 9–11, 2026. Read AGENTS.md and the authoritative artifacts/reports/t3-forge-20260909/implementation-plan.html, plus the independent unified-graph-ui-plan.html. The user's current Forge direction supersedes the older continuity/fork-rehearsal, broad-polish, no-Uniswap and obsolete Luna-development mandates only where explicitly stated. Do not execute those old campaigns.

Use GPT/GLM for development and direct focused checks; the in-app provider is used only to exercise the product's real agent capability. Read .repos/effect-smol/LLMS.md before Effect-heavy changes. Current upstream-merge work is unrelated: preserve it, avoid compatibility archaeology, stage only your intended paths. No PR, push, production deployment or mainnet signing. Public mainnet reads are allowed; only the configured Uniswap public testnet may be written. Do not repurpose Hyperliquid signers or weaken any existing trading guard.

All product numbers and transaction evidence must be real. Fixtures belong only to automated tests. Authentic historical windows are explicitly historical and cannot drive live fee policy. Missing access/data produces a named unavailable state. No Node vm/host import fallback for generated code. No model-authored unrestricted Solidity. One approved fixed hook only.

Use the phase's listed file ownership; treat new names as intended targets, verify the actual repository seams. Existing shared schemas/RPCs must be updated across server, web, desktop and client-runtime; mobile only as needed to avoid broken shared compilation. Add focused tests and targeted package checks. For user-visible changes follow test-t3-app using one retained isolated stack/browser for the entire campaign, owned by the designated tester. Do not test development by asking the application to modify this repository.

Write progress/evidence under /tmp/t3-forge-20260909/U2/ with exact checkout, tested SHA, commands/cwds/exits, actual receipts, redacted screenshots and open failures. Record 'implemented', 'automated verified', 'real-data verified' and 'human accepted' separately. Never mark a phase accepted yourself. When checks pass, commit only the phase implementation and durable behavior docs (not temporary evidence). Return the concrete human verification steps and wait for the human's actual pass/fail before beginning any dependent phase. If the gate fails, repair this phase and repeat its relevant checks. An elapsed timeout is not approval.

U2 — Finish visual and interaction acceptance (inside F6)
This UI phase is part of the main campaign, not extra schedule.

Own only focused UI repairs and tests after U1. Exercise every row of the UI state matrix on the retained isolated app with actual backend events; isolated fault injection may provoke errors, but no fixture defaults survive. Use real Graph and real testnet receipts for success states. Confirm v1/v2 same-window comparison, historical/live transitions, expiry status, revoked policy, reload/reconnect, environment separation and unavailable provider. Verify labels at normal projector zoom, responsive page width, contrast (4.5:1 ordinary text), keyboard focus/escape/return focus, semantic live status announcements, accessible evidence alternatives and reduced motion. Avoid a layout that only works with short IDs or one screenshot. Keep failed cases open and repair the original implementation. Commit only the verified fixes.

Expected result:
A premium experience whose loading, failure and version-transition states are as coherent as its successful screenshot.

Human verification, then stop:
George watches the full demo at 1920×1080 and uses it at 1024px. Without narration, identify current signal version, effective fee and source age; pause with provider stopped; inspect a real swap; return from comparison to Live. Approve U2 only when all applicable matrix states have evidence.
