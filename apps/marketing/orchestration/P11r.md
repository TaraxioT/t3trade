# P11r - Regional re-verification (2026-08-22)

Verifier: P11r worker. Preview: coordinator's server at http://localhost:4180 (fresh `pnpm --filter @t3tools/marketing build`). Shots: `orchestration/shots/p11r/`. Image analysis: agy-vision (`gemini-3.7-flash-high`) only.

Scope note: the hero region (original section A) was EXCLUDED mid-mission by coordinator order (developer approved direction but ordered one more hero revision; Hero.astro being rebuilt concurrently). Hero shots captured before the order (hero-*.png) are retained on disk but carry no verdict. No backdrop-filter injection was needed; no capture hung. One capture-only style injection was used for the composer caret (`animation: none; opacity: 1` on caret elements, applied after load, screenshot only).

## A. HERO - EXCLUDED (under revision; re-check separately)

## B. Mission story scrub (1440x900, 11 stepped frames mission-1440-00..10.png)

agy reads per frame, in scroll order:

| frame | state read |
|---|---|
| 00 | empty plot, Waiting +$0.00, log/positions clean |
| 01 | still empty plot, Planning bar appears ("Plan 76,763 by 5:24 PM"), survey rows fill |
| 02 | history path draws, target line 76,763, "Plan published" log |
| 03 | Armed (amber), watch line 75,778.7 |
| 04 | trigger hit marker on line, "Watch fired 75,784.4" |
| 05 | entry line 75,810.4, capsule "Long 0.0536", position row Open |
| 06 | run begins past entry, all surfaces +$0.00, 0% to target |
| 07 | +$9.17, 18% to target, holding |
| 08 | drawdown dip -$6.84 above wake/stop, rally under way, +$17.97, 35% |
| 09 | 76,763 target reached, Completed +$51.06, 100%, "Sold to close 0.0536 BTC at 76,763 +$51.06", positions Closed/Done |
| 10 | unpin reveal of next section, Mission complete bar persists |

- Beat order: empty plot -> history draws -> Armed -> trigger -> Long at entry -> P&L counts -> drawdown -> rally -> Completed. Holds end to end.
- No element asserts a state the chart has not reached (agy checked per frame).
- No rogue full-viewport green line at focus in any frame (previously fixed regression stays fixed).
- No dead/blank frames.
**PASS** (evidence: mission-1444 typo aside, files `mission-1440-00.png` .. `mission-1440-10.png`)

## C. Reduced motion, forced (rm2-1440-mission-end.png, rm2-390-mission-end.png)

First attempt (rm-*-mission-end.png) overshot the pin and captured the checks section; discarded, re-captured at 0.85 pin progress.

- Desktop 1440: fully composed end state. Chart drawn to 76,763 target chip with entry/wake/stop/reassess chips; feed settled at +24.79% +$51.06, 100%, final row "Sold to close 0.0536 BTC at 76,763 +$51.06 05:24 PM"; positions Closed/Done; capsule "Completed +$51.06", "Mission complete". No blank or half-drawn surface.
- Mobile 390: chart drawn to target with level chips and 2.7:1 box; feed settled on the win row at 100% +$51.06; positions/capsule collapsed per the documented phone-surface rule. No half-drawn surface.
**PASS** (hero rm check excluded with section A)

## D. Composer (composer-caret-zoom.png, capture-only caret forced visible)

- Placeholder transcribed EXACTLY: `Ask anything, @tag files/folders, $use skills, or / for commands` - character-exact incl. the `$` skills sigil.
- Caret: thin vertical bar, vertically centered on the placeholder line. Note: it renders immediately AFTER the placeholder text (at line end), not before it - reads as the composer's own design; vertical alignment is correct either way.
**PASS**

## E. Build guards

- `pnpm --filter @t3tools/marketing build`: green, `scroll timelines ok (34 declarations, none folded)` at guard 34.
- `pnpm --filter @t3tools/marketing exec astro check`: 0 errors, 0 warnings, 2 hints (pre-existing, unrelated script hints).
- Built CSS (`dist/_astro/*.css`): exactly ONE `view-timeline-name: --mission`; source has exactly one, in `src/components/sections/Mission.astro:170`. No duplicate from the hero episode remains.
**PASS**

## F. Pre-Flight boxes touching the non-hero regions

| box | verdict | evidence |
|---|---|---|
| Theme lock (dark, whole page) | pass | every frame read in B/C dark; no light surface |
| Motion motivated (mission beats) | pass | every beat is storytelling/state, verified live in the scrub; no new loops |
| Motion claimed = shown | pass | scrub shows the pinned story animating through all beats |
| Sticky per native CSS scroll() pin | pass | pin held across frames 00-09, unpin at 10 |
| No scroll listener | pass | grep `addEventListener("scroll"` in src = 0 |
| Zero em/en dashes | pass | grep U+2014/U+2013 src = 0, dist/index.html = 0 |
| Reduced motion composed end state | pass | section C |
| One mission timeline | pass | section E (exactly one `--mission` declaration, src + dist) |
| No animation library | pass | grep gsap/framer/motion in src = 0 |
| Marquee max 1 / nav / eyebrow count etc. | unchanged from P11 | no non-hero source touched since P11 except Composer.astro caret fix and Mission timeline count |

## Verdict

**Non-hero regions: PASS. Hero region: EXCLUDED (under revision, re-check separately).**

Observations for the developer (non-blocking):
1. Composer caret sits at line END (after "commands"), not line start. If the intent was a leading caret before the placeholder, say so; otherwise fine.
2. Build guard moved 37 -> 34 with the hero going static; if the hero revision reintroduces timelines, the guard must go back up (never stay lowered).
3. rm first-pass overshoot was a capture-script artifact, not a page defect; the reduced-motion end state itself is fully composed.

## Post-script: Section A completed after hero revision landed (2026-08-22, second pass)

Hero rebuilt to an armed/watch resting state. Fresh `pnpm --filter @t3tools/marketing build`: green,
still `scroll timelines ok (34 declarations, none folded)`. Media forcing used CDP
`Emulation.setEmulatedMedia` (Playwright emulateMedia / --force-prefers-reduced-motion silently fail
in this Chrome build - instrument finding confirmed and honored; no forced-media check relied on the
broken paths).

### A. HERO (hero2-1440.png, hero2-1280.png, hero2-390.png, hero2-1440-scroll100.png, hero2-1440-rm.png)

| check | 1440x900 | 1280x800 | 390x844 |
|---|---|---|---|
| text block present/readable (eyebrow, 2-line headline, subtext, 2 CTAs) | yes | yes | yes |
| both CTAs fully above the fold | yes | yes | yes |
| slicing | deliberate log bottom-row crop ONLY (matches the app's scrolling log; dispositioned deliberate per coordinator) | same | same |
| bottom of frame = soft gradient fade into background, completes above fold | yes | yes | yes |
| instantly parseable as agent watching a market | yes: BTC Armed capsule, Waiting on trigger, price line to plan beat, feed rows, +$0.00/0% | yes | yes |

- Static identity: scroll 0 vs scroll 100 frames are content-identical inside the replica (agy found
  zero content differences; only the fixed background grid's sub-perceptual shine-through shift).
- Reduced motion (CDP-forced), 1440: fully composed static Armed/Waiting state, no half-drawn
  surface, no slicing beyond the deliberate log crop.
- Post-trigger elements confirmed suppressed in the reads: no entry/stop/wake level chips beyond the
  plan's trigger/target, no wedge, no position rows, money reels at +$0.00 / 0%.

**PASS**

### Overall verdict (whole page)

**SHIP-READY.** Sections A (hero, second pass), B (mission scrub), C (reduced-motion mission end),
D (composer), E (build guards at 34, exactly one `--mission` timeline), F (non-hero Pre-Flight boxes)
all PASS on the current build lineage. The only standing observations remain non-blocking: composer
caret renders at line end (vertically centered); keep the timeline guard in step with any future
hero timeline changes.
