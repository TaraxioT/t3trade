# P6: cinematic page (spine, tints, hero, switcher, slop rows)

Status: in progress (updated as work lands).

## Plan

1. Spine: one scroll-drawn price line in the left gutter (fixed SVG,
   `scroll(root block)` timeline, resolves with a terminal node; fully drawn
   under reduced motion / no-support; hidden below 1081px).
2. Hero rebuild: asymmetric split, cropped replica `Window` bleeding off the
   right/bottom, alive on load (settled frame + exempt replica idle loops),
   sticky stage expanding the crop over ~85vh of scroll (pin #1 of 2).
   Text budget: 4 elements, 2-line headline, <=20-word sub, top padding
   <=6rem, CTA above the fold at 1280x800 and 390x844.
3. Section tints: subtle per-section neutral background shifts (same dark
   theme, no second accent).
4. Harness: CSS-only radio switcher (no new JS; contract keeps scripts to
   release lookup + tape). Five tabs, one panel, no re-pinning.
5. Slop rows: 4 (middle dots: hero eyebrow, footer, cta-release), 6 (arming
   pulse -> one-shot state transition), 12 (checks/arming mirrored split ->
   arming becomes a full-bleed state band; hero/CTA centered repeats ->
   hero asymmetric), 15 (repo intent wears "GitHub" page-wide), 16 (chrome
   UI glyphs -> lucide via renderIcon; Apple/GitHub brand marks stay).
6. Eyebrows: drop the near-duplicate Open eyebrow; page total 1 (budget 4,
   self-cap 3).

## Requests filed against frozen files

- icons.ts: add lucide `Check` (and optionally `ArrowUpRight`). Filed with
  the coordinator. Fallback in place: `ShieldCheck` for checkmark glyphs.

## Notes

- Replica binds beats to a named `--mission` timeline; the hero instance
  deliberately does NOT define one, so it rests on its settled frame instead
  of racing the story (avoids double-telling the mission in the hero).
- Pin count target: exactly 2 (hero crop expansion, mission stage).

## Results

Status: complete. (Session was killed mid-verification by a provider network
failure; state was re-audited from this file plus the tree, and verification
was redone against the final build.)

### Section-by-section

- Hero (rebuilt): asymmetric split. Copy column left (eyebrow, 2-line
  headline, 20-word sub, Download + GitHub CTAs), real replica `Window`
  right, cropped by a clip-path container and bleeding off the right edge
  (`margin-right: -72px`) and bottom. Sticky stage (pin 1 of 2) holds for
  60vh of scroll while the crop opens (clip inset 56% -> 0 over the tail's
  view pass) and the copy hands the stage over (fades out by 30%). The tail
  carries two view-timeline names (`--hero-open, --mission`, comma list:
  this Chromium rejects space-separated multi-names) so the console plays
  its own story during the pinned opening. Phone: copy first, 300px console
  slice bleeding off bottom/right, in-flow `--mission` timeline.
- Layout: price-line spine added (fixed left gutter, `scroll(root block)`
  draws the stroke 0-97%, terminal node resolves 95-98%; rests fully drawn;
  >=1081px only). Footer middle-dots removed (3 -> 0).
- Tape: unchanged (the one legal marquee, real data).
- Mission: note ticks swapped to lucide `Check` via `renderIcon` (was
  hand-rolled path); scrub-draw replaced with the shared `check-in` (the
  injected svg has no `pathLength`). Deeper room tint.
- Checks: preview checkmarks swapped to lucide `Check`; the preview pane
  now bleeds off the right frame edge at >=1081px
  (`margin-right: calc(50% - 50vw)`). Tint already #08080a.
- Harness (rebuilt): CSS-only radio switcher. Five tabs (icon + name,
  accent underline on the checked one), one panel (command chip + capability
  tag with lucide Check). Keyboard: radios remain focusable, `:has(:focus-visible)`
  lights the focused tab. Zero JS added. Not pinned.
- Arming (rebuilt): full-bleed two-state band under stacked copy (was the
  mirrored split of Checks). The armed dot now lights ONCE as a view-timeline
  state transition (cover 22-42%, box-shadow + band wash), no infinite pulse.
- State: two equal cards merged into one tile divided by a rule.
- Open: near-duplicate eyebrow removed; "Browse the source" -> "GitHub".
- Cta: "Run from source" -> "GitHub"; release line reduced to 1 middle dot.
- index.astro: unchanged ordering, splice comment for P7's Capture.

### slop.md rows fixed

- Row 4 (middle-dots): hero eyebrow 2->0, footer 3->0, cta-release 2->1
  (max 1/line). Nav tag and tape placeholder each carry 1 (within ration).
- Row 6 (loops): arming pulse replaced by one-shot state transition.
  Remaining infinite animations: tape run + its loading shimmer (exempt)
  and replica-internal idle loops (clause 9 amendment exempts them).
- Row 12 (layout families): checks/arming mirrored split dissolved (arming
  is now a full-bleed band); hero/CTA centered repeats dissolved (hero is
  now an asymmetric pinned split).
- Row 15 (CTA intent): repo intent wears "GitHub" everywhere (nav, hero,
  open, cta, footer). "Download for macOS" unchanged.
- Row 16 (hand-rolled SVG): hero arrow -> lucide ArrowUpRight; mission note
  ticks and checks checkmarks -> lucide Check; harness tag -> lucide Check.
  Remaining chrome SVG: Apple download mark and GitHub mark (brand glyphs,
  allowed).
- Row 2 (near-duplicate eyebrows): Open eyebrow dropped.
- Rows left to later waves by design: 8 (scroll-cue sentence in the Mission
  lede is copy), 9 (release tag is real data), 17 (copy audit).

### Mechanical counts

- Eyebrows: 1 (hero). Budget ceil(10/3) = 4, self-cap 3: pass.
- Layout families (10 sections incl. Capture): hero = asymmetric pinned
  split; tape = marquee band; mission = pinned stage + notes grid; checks =
  copy-left/edge-bleed-pane-right split; harness = switcher; arming =
  full-bleed state band; capture = P7's; state = single divided pane;
  open = narrow editorial column; cta = centered stack. 10 distinct
  families, no repeats.
- Pins: exactly 2 (`position: sticky` in Hero + Mission; nav chrome sticky
  excluded).
- Marquees: 1 (tape).
- Shipped JS: unchanged (1 inline script: release lookup + tape; radio tabs
  are CSS-only).

### Verification evidence

- `pnpm build`: green, guard reports "scroll timelines ok (32 declarations,
  none folded)" (was 24 at P4; count rose, MIN_TIMELINES 12 untouched).
- `astro check`: 0 errors, 0 warnings, 1 pre-existing hint in the frozen
  scripts/extract-app-tokens.mjs.
- No hand-typed app token value introduced: page chrome values only
  (tokens.css untouched; new literals are page-level hexes in section
  styles and the spine's rgba stops, same class as existing page chrome).
- agy-vision verdicts (shots in orchestration/shots/p6/):
  - hero-1440x900: bleeds right, no overlap, headline + both CTAs visible.
  - above-fold-390x844: headline 3 lines, both CTAs visible, console slice
    cropped at bottom; no overflow defects beyond the intended bleed.
  - harness-1440x900 + interaction test: tabbed switcher confirmed, one
    panel, active tab underlined; clicking Codex tab swaps panel to
    "codex login" (DOM-verified).
  - arming-1440x900: full-width two-state band to the right frame edge,
    two dots, armed dot lit and static.
  - cta-1440x900: spine visible in left gutter ending in a lit node;
    buttons "Download for macOS" / "GitHub".
  - checks-1440x900: card bleeds off right edge; check rows show
    lucide checks.
  - hero-open-1440x900: console fills frame width, chart alive, copy gone.
    Residual notes (agent-log internal clipping, appwin-bg wash text) are
    replica internals owned by P5, reported not fixed.
- Above-fold proof: CTA bottom 655px < 800 at 1280x800 (screenshot
  above-fold-1280x800.png), 462px < 844 at 390x844 (above-fold-390x844.png).
- Reduced motion (emulated): spine dashoffset 0 (fully drawn), hero copy
  opacity 1, crop resting on the composed cropped frame
  (above-fold-1280x800-reduced.png).
- Timeline mechanics measured in-page: at scroll 0 clip inset(4%/56%),
  copy 1.0; scroll 300 clip 18.3%, copy 0.52; scroll 540 clip 2.6%,
  copy 0. Smooth monotone, settles.
- Preview server on 4180 started and stopped by tracked PID (20171).
  The pre-existing daemon on 4173 was never touched.

### Requests filed / handoffs

- icons.ts: requested and received `Check` + `ArrowUpRight` (landed by P3).
- To P5 (replica owner): in the hero crop the appwin-bg wash lines and the
  agent-log panel's internal clipping read as "ghost text" at partial-crop
  widths (agy flagged). Fidelity call is P5's.

### Not done / out of scope

- Mission lede scroll-cue sentence and all other copy: left for the copy
  wave per instructions.
- cta-release version tag: kept (real release data, row 9 borderline).
- The curl install box's horizontal scrollbar at 1440: pre-existing,
  deliberate (survives phones without wrapping).
