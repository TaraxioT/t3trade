# P7 progress: real app capture section

## Status: COMPLETE (2026-08-21)

## Contract read
- Read in full before first edit. Relevant clauses honored: at least one real
  capture on the page; `docs/media/t3trade-mission.png` never shipped;
  no perpetual loops in page chrome (drove the still-vs-video decision);
  one accent, dark only, marketing radius ladder, no eyebrow, no em-dashes.

## Still vs video
Still. Contract non-negotiable 9 bans perpetual loops in page chrome (only the
real-data ticker tape is exempt), so a looping muted video of the panel would
violate the contract. The still carries the panel's real glass (backdrop blur
stayed ON) and readable content.

## Harness lifecycle (closed)
- Started 00:15, spawn PID 3391 (child node 3412 held port 5799),
  log /tmp/p7-harness.log, served http://localhost:5799/.
- Stopped 00:21: killed PID 3391, confirmed port 5799 clear and PID gone.
- No fixture edits made under apps/web/harness. The harness page as committed
  already renders the real MissionLivePanel against fixtures; index.html
  already has class="dark". Nothing to report as local-only changes: zero.

## Capture
- Script: apps/marketing/orchestration/p7/capture.mjs (throwaway, not shipped).
  playwright-core 1.60.0 via absolute .pnpm path, executablePath pointed at
  cached chromium-1234 "Google Chrome for Testing" (rev 1223 not cached),
  headless, --disable-gpu, deviceScaleFactor 2, dark class forced, waits on
  [data-testid="mission-live-panel"] visible + ETH text + panelState (no
  sleeps). Element screenshot of the panel root.
- Desktop: 1440px viewport, panel 1406x780 css -> 2812x1562 png, cropped to
  2812x1560. Mobile: 390px viewport, 356x982.5 css -> 712x1966 png, cropped to
  712x1964.
- Backdrop blur stayed on; no hang (P4's issue did not reproduce).

## Encodes (in apps/marketing/public/capture/)
- mission-live-panel-desktop.avif: 43413 bytes, 2812x1560 (libsvtav1 -crf 30,
  libaom-av1 NOT present in this ffmpeg; svt-av1 used, verified ISO Media AVIF)
- mission-live-panel-desktop.webp: 94552 bytes, 2812x1560 (cwebp -q 82)
- mission-live-panel-mobile.avif: 34073 bytes, 712x1964
- mission-live-panel-mobile.webp: 71460 bytes, 712x1964

## agy-vision verification
- Desktop capture: full transcription returned real coherent panel content
  (ETH · USD 1,869.4 +1.84%, entry 1,864.2, stop 1,858.1, target 1,878.2,
  risk bar -$6.00/+$7.00/1.2:1, agent log rows, positions "ETH 5x Long OPEN",
  footer "Holding long / next 12m 31s"). Verdict: real, highly coherent.
- Mobile capture: coherent, no gibberish, no broken/overlapping layout.
- Final AVIF: small text crisp, no compression artifacts.

## Alt text (drafted via agy-vision, on the img)
"Dark-mode crypto trading dashboard for ETH/USD at 1,869.4 (+1.84%), showing a
1m price chart with target and stop levels, an automated agent log of trade
executions, and active 5x leveraged long positions."

## Component
- apps/marketing/src/components/sections/Capture.astro (new, self-contained:
  own scoped styles, own /capture/ assets, no replica imports). picture with
  AVIF+WebP sources, mobile art direction under 640px, width/height 2812x1560
  on img with aspect-ratio reserved per breakpoint (CLS 0), loading="lazy",
  one view() arrival scrub gated by no-preference + @supports. Compiles clean
  via @astrojs/compiler (diagnostics: []). Marketing site builds.

## Recommended splice position
After the Mission (cockpit scroll story) section and before Harness: the page
has just shown the simulated panel beat by beat, so the real capture lands as
the payoff of that story before the conversation moves to agents/CLIs.

## Banned image audit
- public/t3trade-screenshot.webp: ABSENT from apps/marketing/public (confirmed).
- Remaining references to docs/media/t3trade-mission.png (not fixed, per scope):
  - README.md:15 (img tag)
  - marketingredesignprompts.md:138 (mention)
  - scripts/build-t3trade-themed-assets.py:210 (build input map)
  - apps/marketing/DESIGN-CONTRACT.md:64 (the ban itself)

## Not done / notes
- Video not shipped (contract rule 9; see above).
- No marketing dev server started; only the web harness on 5799 (permitted).
