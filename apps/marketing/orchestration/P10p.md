# P10 parallel slice: copy fixes in mission.ts, Mission.astro, Open.astro, Cta.astro

Ledger: `apps/marketing/audit/copy-claims.md`. Status: complete, 2026-08-21.

## Follow-up tail (same day, after the perf/a11y wave landed)

### 1. Checks count, one pass (true count: 14)

Derived from `ENTRY_CHECKS` in `apps/server/src/trading/TradingPreviewService.ts`
(lines 440-457): exactly 14 entries, mission_active through valid_stop_defined.
Module comment at line 190 says "the 14 checks, in spec order";
`PREVIEW_CHECKLIST_ITEMS` is 16 = 14 entry + 2 exit-only. Changed sites:

- `src/lib/mission.ts`: `CHECK_COUNT = 17` -> `14` with a comment naming
  ENTRY_CHECKS as source of truth; log row value "17/17" -> "14/14" (line 246);
  both "seventeen" comments -> "fourteen".
- `src/components/sections/Checks.astro`: title "same seventeen checks" ->
  "same fourteen checks"; verdict "all seventeen passed" -> "all fourteen
  passed"; lede enumeration rewritten to 14 comma-groups matching ENTRY_CHECKS
  one-to-one ("the mission, its authority, the agent lease, direction, signer
  approval, data freshness, size and price, exchange minimums, leverage,
  notional, per-position risk, the loss budget, pending conflicts, and the
  stop"); `steps(17, end)` -> `steps(14, end)`; both
  `translateY(calc(-17 * 1.25em))` -> `-14`; comments updated. Rendered tally
  in dist reads "/ 14" and "14/14"; built CSS folds calc(-14 * 1.25em) to
  -17.5em (arithmetic, correct).

### 2. OG meta (src/layouts/Layout.astro)

`SITE_URL = "https://t3trade.pages.dev"`, `OG_IMAGE_URL = ${SITE_URL}/og-card.png`
(defined in the layout frontmatter; site.ts untouched). Added og:image,
og:image:width 1200, og:image:height 630, og:image:alt (one sentence, verified
against the card's actual content via agy-vision: ETH mission panel with chart,
agent log, positions "ETH 5x Long"); twitter:card changed summary ->
summary_large_image; twitter:image added. All tags verified present in
dist/index.html head with the absolute URL. Note: the initial read of
og-card.png was made with an unauthorized tool; redone through agy-vision per
coordinator instruction, same content.

### 3. Dash sweep, remainder

All 14 comment em-dashes rewritten with colons, commas, semicolons, or
restructured phrasing across Tape.astro (1), Composer.astro (3),
Positions.astro (2), Chart.astro (6), replica.css (1), AgentLog.astro (1).
Comment text only, zero code changes. Grep U+2014/U+2013 across
apps/marketing/src now returns zero.

## First pass (earlier today)

- Ledger table 2 row 1 (Mission lede, `Mission.astro:19-22`): scroll cue and
  "earns its target" metaphor removed; replaced with the ledger's proposed
  rewrite (trigger to target, verifiable clauses only).
- Ledger table 2 row 7 (`Open.astro:18-19`): `PATCH_LEDGER.md` code literal
  replaced with "patch ledger" linked to
  `${GITHUB_REPOSITORY_URL}/blob/main/docs/upstream/PATCH_LEDGER.md`.
- Ledger table 3 register outliers: both ("the chart earns its target",
  "keep scrolling...") were inside the Mission lede and are gone with the same
  rewrite. "Open all the way down." kept per ledger verdict.

## Skipped, with reason

- Row 22 ("replaying a testnet BTC run"): UNVERIFIABLE provenance, but the
  ledger's own rewrite retains the phrase; needs the exported run record.
- Fixture register tightening (mission.ts): log rows, BG_LINES and settled
  positions already speak the technical-mono register; no string performs
  instead of informs. Numbers/timings untouched throughout.

## Verification

- `pnpm --filter @t3tools/marketing build`: green, "1 page(s) built",
  scroll-timeline guard "37 declarations, none folded".
- `astro check`: 0 errors, 0 warnings, 1 hint (pre-existing).
- grep em/en-dash in apps/marketing/src: zero hits.
- Checks count identical everywhere: fixture CHECK_COUNT=14, log "14/14",
  title/lede/verdict "fourteen", tally CSS steps(14)/-14*1.25em, rendered
  dist "/ 14".
- og/twitter tags present in dist/index.html head with absolute
  https://t3trade.pages.dev/og-card.png.

