# P10a - copy claims audit (read-only)

Status: COMPLETE. Ledger at `apps/marketing/audit/copy-claims.md`.

Key findings while working:

1. "Seventeen checks" is FALSE everywhere it appears. The server's entry
   checklist (`apps/server/src/trading/TradingPreviewService.ts:442-457`,
   ENTRY_CHECKS) has 14 items and its own comment says "the 14 checks".
   Affected: Checks.astro title/tally/verdict note, mission.ts CHECK_COUNT and
   the "17/17" log row. The Checks lede also enumerates 15 items, matching
   neither 17 nor 14.
2. The price tape is labeled "Hyperliquid testnet markets"
   (Tape.astro:10) but is filled from the MAINNET API
   (index.astro:58, api.hyperliquid.xyz). Mainnet prices under a testnet label
   on a trading page. Either swap the endpoint or the label.
3. GitHub releases for TaraxioT/t3trade are currently EMPTY, so both download
   buttons fall back to an empty releases page. Copy is accurate; distribution
   is not. Needs whoever owns releases.
4. Everything safety-critical that could be checked verified TRUE with
   file:line evidence: fail-closed signer, local signing, key never persisted,
   exchange-side reduce-only stops, 8-trigger reconciliation, §16.2 loss
   budget, controls that work with the harness offline, three-of-five
   providers trading, login commands, install script, app name, alt text
   (confirmed against the capture image via agy-vision).
5. One UNVERIFIABLE: the "replaying a testnet BTC run" fixture provenance;
   needs the exported run record.
6. Em-dashes: 14 total, all in code comments, 0 in visible strings
   (non-negotiable 1 holds). 0 en-dashes.
7. String defects: the Mission scroll-cue/"chart earns its target" sentence
   (rewrite proposed), the checks-count strings above, the tape label, and a
   link suggestion for PATCH_LEDGER.md (it lives at docs/upstream/).

No repo file was modified. P8 was editing components during the read window
(Hero 01:38, Mission 01:40, AgentLog 01:25 mtimes); line numbers are against
the latest on-disk state at audit time.
