# P11 - Final verification sweep (2026-08-21)

Verifier: P11 worker. Preview: coordinator's server at http://localhost:4180 (dist, fresh build).
Shots: `orchestration/shots/p11/`. Image analysis: agy-vision (`gemini-3.7-flash-high`) only.
Note: fixed-scroll stepped captures at viewport size, never fullPage; no backdrop-filter
injection was needed (no capture hang). Tape band is live data and differs between frames.

## 1. Desktop scrub (1440x900, 21 frames desktop-00..20.png)

Beat first appearance (agy-vision reads, frames in scroll order):

| beat | first frame | evidence in frame |
|---|---|---|
| survey | 05 | log fills with pre-mission rows (Stood aside, Journal note, Watch armed) |
| plan | 05 | target line 76,763 drawn, status "Planning" |
| armed | 06 | capsule "Armed", amber trigger mark at 75,778.7 |
| trigger | 06 | trigger circle + "75,778.7 above" chip |
| entry | 07 | entry line 75,810.4, capsule "Long 0.0536" |
| stop | 07 | stop line 75,460.9, positions row "Working 75,460.9" |
| run | 07-08 | position opens, price walks, P&L reel moves |
| wedge | 08 | cyan projection cone on chart |
| drawdown | 08 | -$4.56, dip dot -$6.84, wake 75,634.5 |
| recovery | 08-09 | price climbs 75,981 -> 76,763 |
| target | 09 | 76,763 hit, "76,763 target" badge |
| settle | 09 | Completed, "Sold to close 0.0536 BTC at 76,763 +$51.06", feed settles |

Order holds end to end; no element asserts a state the chart has not reached (status wording
matched the chart in every frame read); no dead frames (every frame had rendered content).
Frames 00-04 are the hero crop (replay start: empty chart, Waiting) then the mission section
entry - the empty chart at 03-04 is the pre-survey state, in order. **PASS**

## 2. Mobile scrub (390x844, 21 frames mobile-00..20.png)

Same story in order: 00 hero crop (Armed), 02-03 Waiting + survey rows, 04 Armed, 05 entry
("Preview passed 14/14", "Bought to open"), 06 run (+$9.16, 18% to target), 07 settle
(100%, "Sold to close"). No out-of-order beat, no dead frame. **PASS** with notes:
- Agent-log descriptions truncate with ellipsis at 390px - data-dense deliberate, readable.
- "OpenCode" CLI tab clipped at right edge: tab strip is `overflow-x: auto` (scrollWidth 537
  vs client 333) - scrollable, not broken.
- `document.documentElement.scrollWidth` = 390: **zero horizontal page overflow**.
- agy reported "75.981.43" duplicate-dot prices: misread at small size. DOM verified all
  prices use comma thousands separators ("75,981.36", "75,810.4").
- Positions table edge clipped in the replica on mobile: the replica window is wider than
  390 and cropped, matching the app-window presentation; positions card + statusbar are
  `display: none` under 820px, documented deliberate in `src/components/replica/Window.astro`
  ("The phone's deliberate surface" comment). **PASS**

## 3. Reduced motion (rm-desktop/mobile-top/end.png + DOM under CDP emulation)

- Both `-top` frames fully composed (agy): chart line to 76,763 with level chips
  (target/entry/wake/stop/reassess), agent log at 100% with settle rows, capsule
  "Completed +$51.06".
- DOM under `prefers-reduced-motion: reduce`: statusbar includes "Mission complete",
  chips "76,763 target / 75,810.4 entry / 75,460.9 stop / 75,634.5 wake", positions
  "Closed"/"Done", money figures "+$51.06" and "+24.79%", log ends "Sold to close ...
  05:24 PM". No blank or half-drawn surface. **PASS**

## 4. Fidelity diff vs p11a reference

Computed-style diff (replica in page vs `p11a/app-styles.md`), both viewports:

| surface | app reference | replica | verdict |
|---|---|---|---|
| panel container | 0px radius, transparent, -apple-system 16px | identical | match |
| glass card (chart/statusbar) | 14px, border rgba(255,255,255,0.12), bg color(srgb .068../0.58), blur(16px) saturate(1.08) | identical on all five values | match |
| level chip | ui-monospace 10.5px, pill radius, border oklab(...0.03), bg .62, blur(8px) | identical | match |
| status bar text color | oklch(0.97 0 0) | identical | match |

agy-vision desktop pair (app-1440 vs desktop-08): all 11 dimensions judged, 10 "match",
number formatting character-exact in style (commas, decimals, +$/% signs). Verdict: faithful.
agy-vision mobile pair: matches on type, radii, borders, glass, palette, log rows, number
format. Divergences found and dispositioned:
- positions card + status bar absent on mobile: **documented-deliberate** (Window.astro
  phone-surface rule; audit/fidelity.md post-P5 note covers the cropped surface).
- log timestamps absolute ("03:44 PM") vs app's live relative ("8m ago"): replay-content
  difference, not design system; informational only, unrowed in fidelity.md (P5 note).
fidelity.md dispositions re-checked in source: rows 3,4,5,6,7,11,12,19,23,24,26 FIXED
(mission.ts, icons.ts with ArrowUp incl. Composer.astro:41 using ArrowUp, token-sourced
values), rows 1,2,13,18,21,25 deliberate, rest affirmed. One remaining defect candidate
from the agy desktop diff (title-case "Unrealised" vs app uppercase) is a vision misread:
CSS text-transform uppercase is applied (DOM scan shows rendered uppercase legends).

## 5. Runtime health

- Console: 0 errors, 0 warnings (whole session). **PASS**
- Network: 2 dynamic requests, both 200 (api.github.com releases, api.hyperliquid-testnet.xyz
  info). Zero failures. **PASS**
- Tape: fetches `api.hyperliquid-testnet.xyz/info`, renders real midPx/prevDayPx via
  toLocaleString; frames showed live values. No fabricated price path. **PASS**

## 6. Coherence invariants

- (a) Network coherence: tape -> hyperliquid-testnet; nav tag "testnet - alpha"; footer
  "Hyperliquid testnet only" ("testnet-only key" copy); meta description "on Hyperliquid
  testnet". Only "mainnet" mention is the negative "No mainnet support of any kind".
  No disagreement. **PASS**
- (b) Checks count: "/ 14" x1, "14/14" x2, "all fourteen passed", "fourteen checks." in
  rendered HTML; lede enumerates exactly 14 items (mission, authority, agent lease,
  direction, signer approval, data freshness, size and price, exchange minimums, leverage,
  notional, per-position risk, loss budget, pending conflicts, stop) matching the 14
  ENTRY_CHECKS at TradingPreviewService.ts:440-457. Build guard prints "14 beats" (that is
  the story beats; checks = 14 separately verified). **PASS**
- (c) OG/Twitter: og:type/title/description/image (https://t3trade.pages.dev/og-card.png,
  1200x630, alt), twitter:card summary_large_image, twitter:image same absolute URL. **PASS**
- (d) U+2014/U+2013 across `apps/marketing/src`: 0 hits; also 0 in dist/index.html. (agy
  "en-dash" reports were misreads of plain hyphens; verified against HTML bytes.) **PASS**
- (e) `oklch(` in src/components/replica and src/styles/replica.css: 0 hits; present only in
  generated `dist/_astro/index.BbKkUy5t.css`. **PASS**
- (f) Budgets: 10 sections; 1 eyebrow (`eyebrow hero-eyebrow`) <= ceil(10/3)=4 (DOM scan of
  rendered small-caps labels found only data labels outside the hero); layout families:
  hero split, tape band, pinned mission scroller, sticky checks terminal, tabs+terminal,
  status-card pair, two-column ledger, image capture, open-source split, CTA - >= 4 distinct;
  exactly 2 sticky pins (mission-sticky, checks-sticky); exactly 1 marquee (the tape).
  **PASS**

## 7. Pre-Flight Check (design-taste-frontend Section 14)

| box | verdict | evidence |
|---|---|---|
| Brief inference / dials / system | pass | DESIGN-CONTRACT.md Design read + dials 7/8/6; aesthetic honest (native CSS, no lib) |
| Zero em/en dashes | pass | grep 0 in src and dist; frames re-checked via HTML bytes |
| Page theme lock (dark) | pass | all 42 frames dark; no light section |
| Color consistency (one accent) | pass | hue-160 accent in chrome; profit/loss/armed only inside replica/data marks (frame reads) |
| Shape consistency (two documented ladders) | pass | chrome ladder vs app ladder (fidelity rows 1-2 deliberate) |
| Button contrast | pass | primary CTA dark text rgb(4,18,11) on light accent fill; nav/CTA heights 51px, single line |
| CTA wrap | pass | "Download for macOS" 217x51, one line |
| Form contrast | n/a (no forms) | - |
| Serif discipline | pass | no serif |
| Premium palette | n/a | - |
| Italic descenders | pass | replica italics are 12px log text with normal leading (frames) |
| Hero fits viewport | pass | h1 2 lines; subtext 19 words; CTAs visible at 1440x900 and 390x844 (frames 00 both) |
| Hero top padding <= 6rem | pass | computed paddingTop 0px |
| Hero stack <= 4 text elements | pass | eyebrow + h1 + sub + CTAs (extra <p>s are sr-only mission descriptions) |
| Eyebrow count mechanical | pass | 1 <= 4 |
| Split-header ban | pass | section headers are stacked (frame reads) |
| Zigzag cap | pass | no image+text splits repeated; families vary |
| Duplicate CTA intent | pass | one "Download for macOS" label everywhere; GitHub secondary |
| Logo wall | n/a | none |
| Bento diversity / cell count | pass | checks cards + today/not-today ledger have visual variation, no empty cells |
| Copy self-audit | **FAIL** | "on thereleases page." missing space (sec-cta, dist HTML) |
| Motion motivated | pass | every animation is beat storytelling / state; only loops: tape (real data) + composer caret (exempt fidelity) |
| Marquee max 1 | pass | 1 (tape) |
| Nav one line, <= 80px | pass | 61px, no wrap |
| Section layout repetition | pass | >= 4 families, no repeats |
| Long lists UI | pass | 14 checks use terminal checklist, not divide-y ul |
| Real images | pass | sec-capture carries the real panel capture; replica is the sanctioned 1:1 system |
| No pills on images / photo credits / version footers / micro-meta / hero strip / floaters / scoring bars / locale strips / scroll cues / hero version / section numbers / decorative dots / border-t rows | pass | absent across all 42 frames + HTML greps |
| Content density | pass | short heads, <= 25-word subs |
| Quotes <= 3 lines | n/a | no quotes |
| Motion claimed = shown | pass | scroll-driven beats verified live in both scrubs |
| Sticky per canonical skeleton | pass | native CSS scroll() pinning, 2 pins, start held across frames 04-09 |
| No scroll listener | pass | grep addEventListener("scroll") = 0 |
| Reduced motion composed end state | pass | section 3 |
| Dark mode tokens | pass | single dark theme, token bridge |
| Mobile collapse explicit | pass | Window.astro phone rules; scrollWidth 390 |
| Viewport stability | pass | appwin uses 100dvh calc; no h-screen jumps observed |
| Empty/loading/error states | pass | tape self-hides on failure; release lookup falls back to GitHub URL (script read) |
| Cards where needed | pass | data breathes in terminal/list layouts |
| Icons from library | pass | lucide set via src/lib/icons.ts, app geometry |
| Client-leaf isolation | n/a | Astro, 1 inline script 1165 bytes gzipped (< 12kB budget) |
| AI tells | pass | none found |
| Core Web Vitals plausible | pass | static page, 1 CSS file, 1.1kB JS, no webfonts blocking (system + hosted) |
| One design system | pass | native CSS + app token bridge only |

## 8. Build guards

- `pnpm --filter @t3tools/marketing build`: PASS - token check ok, beat invariants ok
  (14 beats, order holds), `scroll timelines ok (37 declarations, none folded)` (>= 37 guard).
- `pnpm --filter @t3tools/marketing exec astro check`: 0 errors, 0 warnings, 1 hint
  (unrelated palette-set hint in a script). PASS.

## Failures

1. **P10 (copy)**: sec-cta release sentence renders "on thereleases page." - missing space
   before the link. `dist/index.html`: `checksums are on the<a href=".../releases">releases
   page</a>.` Source: `src/components/sections/` CTA section. Visible on desktop and mobile
   (mobile-20 frame, agy independently flagged it). Trivial fix, but it is a visible broken
   string, so it blocks ship until fixed.

## Re-verification after P10 fix (2026-08-21, post-sweep)

| Check | Verdict | Evidence |
|---|---|---|
| Copy self-audit: "thereleases" typo | **PASS (fixed)** | DOM read at the live CTA: `document.body.innerText` contains "the releases page", zero hits for "thereleases" (served from rebuilt dist). agy-vision transcription of `shots/p11/cta-fixed-1440.png`: "Older builds and checksums are on the releases page." with proper space and the link intact. |

No other file changed; all other rows stand as passed.

## Verdict

**SHIP-READY.** The single blocker (the "thereleases" typo, P10) is fixed and re-verified
both by DOM read and by visual transcription of a rendered frame. Every other row of this
sweep passed on the same build lineage: story coherence both viewports, reduced-motion end
state, fidelity (faithful; mobile exclusions documented-deliberate), runtime health, all six
coherence invariants, the full Pre-Flight Check, and both build guards.
