# P2 - index.astro component split (pure move)

Status: DONE

## What moved where

- `src/lib/mission.ts` - the whole mission fixture, typed: MISSION, derived figures
  (risk/reward/open/rr/dipPnl/DAY_OPEN), candle series + PLAN + GRID_PRICES,
  pnl/pct/roi/mark/chg arcs, LOG (40 rows), EARLIER_TURNS, SIDE/SIDE_THREADS/THREAD
  (incl. capsulePnl), BG_LINES(_RIGHT), POS_TIMES, SETTLED_POSITIONS, STATUSBAR,
  COMPOSER, CHECK_COUNT/PREVIEW_CHECKS/CONTROLS, span() and kineticWords().
- `src/lib/format.ts` - formatters ported from apps/web tradingPresentation.ts
  (formatPrice=formatPrice, formatSignedUsd, formatSignedPercent, formatChangePercent,
  formatNotionalUsd). The zero-frame behaviour ("+$0.00"/"+0.00%") is preserved from
  the page's original inline formatters so no rendered string changes; documented in
  the file header.
- `src/styles/tokens.css` (global, imported by index.astro) - section frame,
  .section-title/.section-lede, inline code chips, .tile spotlight, .tick success mark,
  @keyframes check-in, 820px section/title overrides. The 820px container override is
  scoped `.main .container` (one class up) because imported css bundles BEFORE the
  layout's own `.container` rule; same elements, same values.
- `src/styles/replica.css` (global, imported by replica/Window.astro only) - replica
  machinery that spans components: card-glass trio, .card-legend, status-window/reel/
  track/step/dot machinery + status-walk keyframes, pnl/roi/mark/pct reel machinery +
  pnl-count keyframes, .side-ico* sizes (original relative order preserved), the
  cross-component slices of the mission scroll-timeline layer (big contain-range list,
  full-range trio, `.mission-sticky .tick path`), and the 1080px cover-range lists.
- sections/ (each with scoped `<style>` incl. its media/scroll-layer/reduced-motion
  slices): Hero, Tape, Mission (header copy, scroller/sticky/dim, notes; composes
  Window), Checks, Harness, Arming, State, Open, Cta.
- replica/ (scoped styles per surface): Window (appwin root + tokens, titlebar,
  body grid, main column, minimap, appwin-bg, panel-shell/panel-row/chart-col),
  Sidebar, Topbar (breadcrumb + capsule), Chart (geometry, generated keyframes via
  `<Fragment set:html>` kept intact, marks, chips, rr strip), Positions, AgentLog,
  StatusBar, Composer (wakeup capsule + composer).
- `index.astro` - 12 imports + section order + the unchanged client script
  (release lookup, Hyperliquid tape, tile spotlight). 142 lines total.

Nesting note (bug caught and fixed): wakeup-cap and composer are children of
`.appwin-main`, NOT of `.panel-shell` - matches the original tree exactly.

## Verification evidence

- `pnpm --filter @t3tools/marketing build`: passes; `scripts/check-scroll-timelines.mjs`
  reports "scroll timelines ok (24 declarations, none folded)" (baseline was 16; the
  count rose because the shared @media/@supports wrappers now exist per component -
  the guard is a minimum and was not lowered).
- `pnpm --filter @t3tools/marketing exec astro check`: 0 errors, 0 warnings, 1 hint
  (pre-existing, in scripts/extract-app-tokens.mjs, owned by P3).
- Dev server: an `astro dev` daemon (PID 96533, port 4173) was already running in this
  package - NOT killed (not mine). All screenshots were taken against
  `astro preview --port 4180` over production builds instead; preview PID tracked and
  stopped at the end.
- Before/after full-page screenshots (Playwright, fullPage, css scale):
  `shots/p2/before-1440.png` / `after-1440.png` (both 1425x7876) and
  `before-390.png` / `after-390.png` (both 375x9263) - identical dimensions.
- Pixel diff, 1440: only the live ticker tape band (y 618-629) differs - live
  Hyperliquid prices, expected.
- Pixel diff, 390: tape band plus three <=1px sub-pixel rasterisation shifts
  (dip label glyphs ~0.25px, wakeup pill 1px, one 6px speck); agy-vision confirmed
  these are anti-aliasing/rounding noise, no layout shift.
- agy-vision (per coordinator rule) verdicts on mobile crops (top/mid/bottom):
  IDENTICAL, IDENTICAL (identical MD5), IDENTICAL.
- Fixed-scroll viewport suites (16 desktop + 19 mobile offsets, shots/p2/fixed/):
  pixel-identical at every offset except the tape band, the 2s arming-dot pulse
  animation phase (infinite loop, non-deterministic capture), and a <=1px
  mid-animation log-row shift in two mobile shots.
- DOM geometry audit at matched scroll states (desktop surface and mobile):
  every element height/position measured equal to the original build
  (page heights 7876/7876 and 9263/9263; all section heights equal).

Visual value changed: NO. The only rendered-string deltas between captures are live
exchange prices in the tape, which are fetched at runtime.

## Not done / notes for later waves

- FullPage screenshots of this page are not a perfectly stable instrument: the pinned
  cockpit's rendered state resolves scroll-driven animations differently under
  captureBeyondViewport (proved by measuring the original build at a 7876px-tall
  viewport, where the original itself renders differently). The fixed-scroll suite is
  the authoritative comparison and is pixel-clean.
- All 22 em-dashes live in moved code comments, characters untouched (P10 owns them).
- tapeMarkets stays in Tape.astro and harnesses in Harness.astro (section content,
  not mission fixture).
