# P5: cockpit replica reads as the product

Ledger: `audit/fidelity.md` (dispositions appended per row). Scope: replica
components, replica.css, mission.ts, format.ts. beats.ts frozen.

## Plan

1. Rows 3/4 (high): every hand-drawn svg path and unicode glyph in the replica
   replaced by `renderIcon` from `src/lib/icons.ts` at the app's sizes
   (log rows 11px/2; chart chips 9px/2; status bar 11-12px/2).
2. Row 26 (high): verified - every figure traces to mission.ts; close.
3. Row 7 (medium): wash stops -> `--app-mission-chart-wash-top/mid` (0.13/0.04).
   Row 24 (medium): capsule P&L becomes a `pnl-track` reel over `pnlArc` on
   `beatRange("run","settle")` (existing beats, no new timing).
4. Crop: delete Sidebar.astro entirely; Topbar keeps only the mission capsule;
   composer keeps placeholder + model chip + caret + send; dock icons,
   Initialize Git, Show N more, breadcrumb + split control cut. Window gains
   rounded physical frame (tinted layered shadow, lit top edge) on the desktop
   surface; panel takes the reclaimed width.
5. Token re-source: `app-tokens.generated.css` imported by Window.astro; card
   glass, inks, radii, fonts, wash/wedge stops all via `--app-*`.
6. Reduced-transparency solid fallback for the glass panes.

## Log

- Reading pass done (contract, ledger, all replica files, beats, icons,
  generated tokens, Mission.astro container, guard script). Guard is
  `MIN_TIMELINES = 12` floor; removing sidebar/titlebar elements is safe.
- Found: `app-tokens.generated.css` was not imported anywhere; Window.astro now
  imports it (same mechanism as replica.css).
- App source check (MissionPriceChart.tsx ~855): the wash gradient has THREE
  stops (0% top, 45% mid, 100% 0) - the ledger's "two stops" note was wrong;
  the mock's structure was right, only the opacities were off.

## Completed edits

- mission.ts: `LogRow.glyph` is now `IconName` (12 distinct lucide glyphs);
  removed a duplicated survey-row pair; removed SIDE/SIDE_THREADS/THREAD with
  the surfaces they fed; BG transcript lines derive their size figure from
  `MISSION.size`.
- Sidebar.astro deleted (file + import + status-walk-side keyframes + dead
  .side-* rules in replica.css).
- Window.astro rewritten: imports app-tokens.generated.css (it was previously
  imported nowhere); fonts via --app-font-sans/mono; all inks re-sourced
  (--app-foreground/muted/border/card/profit/loss/armed/info/primary;
  --fg-panel via the app's own 94% mix formula); desktop surface is now an
  elevated rounded canvas (min(1440,100vw-72) x min(900,100dvh-64)) with
  layered tinted shadow + lit top edge from --app-mission-panel-bevel; no
  neon target glow (static elevation instead); behind-glass transcript
  hidden below the desktop surface (it rendered as raw italic text on the
  stacked layout).
- Topbar.astro rewritten: capsule-only (no breadcrumb, no actions, no
  Initialize Git, no split control); P&L tail is a pnl-track reel over
  pnlArc on beatRange("run","settle"); middle phase dot is a foreground
  ring; state word shows size while long ("Long 0.0536").
- Chart.astro: wash stops 0.13/0.04 via --app-mission-chart-wash-* (three
  stops, matching the app's 0%/45%/100% structure; the ledger's "two stops"
  note was wrong); wedge stops via --app-mission-chart-wedge-*; level dashes
  2 4 (entry 5 4); chip captions 9.5px/70%; chip glyphs are lucide (Radar,
  ShieldCheck, Clock) at 9px/2; grid labels via formatPrice; rr-note wraps
  on mobile.
- AgentLog.astro: glyphs rendered via renderIcon at 11px/2; value column
  one step quieter (--fg-muted).
- StatusBar.astro: FileText / TrendingUp / ExternalLink via renderIcon at
  11px/2; glass from the token block.
- Composer.astro: AlarmClock / ChevronDown / Gauge via renderIcon; cropped
  the Low + Full-access chips, the hand-drawn provider mark and the wakeup
  chevron; send button renders ChevronUp pending an ArrowUp glyph.
- Positions.astro: working-ring amber derived from var(--warn) mix (no
  hand-typed oklch); state token hidden at <=820px (was overlapping the
  price column).
- replica.css: card glass fully token-sourced; solid
  prefers-reduced-transparency fallback incl. the status bar; dead sidebar
  icon rules removed.

## Verification

- `pnpm build`: ok, scroll timelines ok (32 declarations, none folded).
- `astro check`: 0 errors, 0 warnings (pre-existing hints only).
- Completion greps: oklch( in replica -> no matches; DM Sans|JetBrains ->
  no matches; unicode glyph set -> no matches; the only literal digit in
  markup text is the "3" in "T3 Trade".
- Shots in orchestration/shots/p5/: desk/mob stepped 00/25/50/75/100, frosted
  and -flat (flat injects backdrop-filter/filter:none for reading only).
  Before-state reference: orchestration/shots/p4/ (P4's frames, this wave's
  starting point; P6's section edits may also differ there).
- agy-vision verdicts: window reads as elevated rounded surface with shadow;
  log rows show line-art icons, no unicode; capsule P&L walks in lockstep
  with the log figure (+$4.59 at desk-75) and lands on +$51.06; no broken or
  overlapping UI on desktop; mobile re-check after fixes: no garbled
  transcript block, no state-word overlap, rr-note wraps.

## Icon requests (to coordinator)

The generated set lacks: ArrowUp (composer send; ChevronUp interim), Lock
(Full access chip, cropped meanwhile), ChevronRight (wakeup tail, cropped).
A rerun of scripts/extract-app-tokens.mjs with these lucide names would let
the composer restore the cut affordances faithfully.

## Delta: requested glyphs landed (follow-up)

ArrowUp, Lock and ChevronRight were added to src/lib/icons.ts (28 glyphs
now). Composer affordances restored faithfully, all via renderIcon at the
replica's established geometry (strokeWidth 2 throughout):
- send button: ArrowUp at 14px, replacing the interim ChevronUp;
- "Full access" permissions chip restored, led by Lock at 16px;
- wakeup pill tail chevron restored as ChevronRight at 12px.
No timing, token or layout rules touched; frozen files untouched.

Verification: pnpm build ok (scroll timelines ok, 32 declarations); astro
check 0 errors; composer-region stepped captures at scroll 0/50/75 in
orchestration/shots/p5/composer-{00,50,75}.png (flat, reading-only), read
via agy-vision: padlock-led Full access chip, down-chevron model chip,
stemmed ArrowUp in the circular send button, ChevronRight closing the
wakeup pill, no misalignment or clipping. Icon requests are now fully
resolved; no outstanding requests.
