export const DISCONNECTED_COMPOSER_PLACEHOLDER =
  "Ask for changes, send follow-ups, or attach images";

/**
 * The one line a new, empty thread carries in this workspace.
 *
 * Trading is not a mode the user switches into and there is no button that
 * starts it, so an empty thread has to say that asking is the way in. One
 * sentence, on the two surfaces an empty thread can be: the draft hero and the
 * timeline placeholder.
 */
export const WORKSPACE_TRADING_SIGNPOST =
  "You can trade, watch markets, and set alerts right here by asking.";
